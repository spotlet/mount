#!/usr/bin/env bash

info () {
  echo "info: ${@}"
}

debug () {
  if ! test -z ${DEBUG}; then
    echo "debug: ${@}"
  fi
}

error () {
  echo >&2 "error: ${@} "
}

fatal () {
  error "${@}"
  exit 1
}

